# Mem0 官方 Claude Code/Codex 插件本地自托管改造计划书

## 一、项目目标

基于 Mem0 官方仓库中的 `integrations/mem0-plugin` 进行二次开发，将当前依赖 Mem0 Platform Cloud 的 Claude Code/Codex 插件，改造成由插件自动安装和管理的本地记忆系统。

截至 2026 年 8 月 5 日，官方插件清单版本为 `0.2.13`，现有插件包含 Claude/Codex 清单、MCP 配置、生命周期 Hooks、Python 脚本、Skills 和测试目录。

改造后必须满足：

1. Embedding 模型独立配置，允许远程 API、本地模型或自定义 Provider。
2. 记忆提取、分类、总结由独立 Memory LLM 完成，不使用 Claude Code/Codex 宿主 Agent。
3. 插件自动读取对话增量，自行生成事实记忆和会话摘要。
4. 支持通用记忆和工作区记忆，并保证工作区之间不能互相召回。
5. 不使用 Docker、PostgreSQL、Mem0 Dashboard 或本地模型作为默认依赖。
6. 用户只安装一次插件，不需要手动下载或启动独立服务。
7. 保留官方插件的 Skills、Hooks 和 MCP 使用体验，尽量兼容原有工具名称。

---

## 二、确定的目标架构

```text
Claude Code / Codex
        │
        ├── Lifecycle Hooks
        │     ├── SessionStart
        │     ├── UserPromptSubmit
        │     ├── Stop
        │     └── PreCompact
        │
        └── Local MCP Proxy
                   │
                   ▼
        Plugin-managed Memory Daemon
        ├── Local HTTP API
        ├── MCP tool implementation
        ├── Mem0 Python Library
        ├── Recall pipeline
        ├── Capture and summarization pipeline
        ├── Workspace isolation
        ├── Qdrant Local
        └── SQLite state/history
                   │
          ┌────────┴────────┐
          ▼                 ▼
    Configurable LLM   Configurable Embedding
      remote/local        remote/local
```

### 运行原则

* 插件与服务使用同一个安装包交付。
* Memory Daemon 是独立常驻进程。
* 插件自动创建 Python 虚拟环境、安装依赖、启动和复用 Daemon。
* 所有 Qdrant Local 和 SQLite 操作只能由 Daemon 执行。
* Hooks 和 MCP Proxy 不直接打开数据库。
* 服务仅监听 `127.0.0.1`，不得暴露到局域网。
* 程序目录与数据目录分离，插件升级不得删除记忆数据。

Mem0 OSS 支持直接通过 `mem0ai` Python Library 嵌入应用，并允许分别配置 LLM、Embedder 和 Vector Store。Qdrant 支持本地路径持久化，因此不需要单独启动数据库服务。

---

## 三、官方源码现状与必须替换的云端依赖

当前官方插件存在以下云端绑定：

1. `.mcp.json` 和 `.codex-mcp.json` 指向 Mem0 官方远程 MCP 服务。
2. `_search.py` 直接调用 Mem0 Platform 搜索接口。
3. `auto_capture.py` 和 `capture_session_summary.py` 直接向 Mem0 Platform 写入记忆。
4. `on_session_start.sh` 直接读取云端记忆数量，并依赖 `MEM0_API_KEY`。
5. `_identity.py` 和 `_identity.sh` 将身份和 Mem0 Cloud API Key 绑定。
6. 部分 Skills 依赖 Platform 的异步事件、categories、统计和全局搜索语义。

改造完成后，除迁移说明和测试样例外，运行代码中不得出现：

```text
api.mem0.ai
mcp.mem0.ai
MEM0_API_KEY
```

---

## 四、新增服务目录

在 `integrations/mem0-plugin` 下新增：

```text
service/
├── __init__.py
├── daemon.py
├── bootstrap.py
├── client.py
├── config.py
├── schemas.py
├── security.py
├── memory_engine.py
├── recall_engine.py
├── capture_engine.py
├── scope_router.py
├── transcript_reader.py
├── state_store.py
├── mcp_proxy.py
├── provider_factory.py
└── migrations/
```

### 各模块职责

#### `daemon.py`

负责：

* 启动本地 HTTP 服务；
* 初始化唯一的 Mem0 实例；
* 持有 Qdrant Local；
* 持有 SQLite 连接；
* 暴露健康检查、召回、捕获和管理接口；
* 实现优雅关闭和异常恢复。

#### `bootstrap.py`

负责：

* 判断 Daemon 是否已经运行；
* 使用文件锁保证只启动一个实例；
* 检查服务版本；
* 启动后台进程；
* 写入 PID、端口和本地认证令牌；
* 服务异常时执行一次安全重启。

#### `mcp_proxy.py`

作为 Claude Code/Codex 启动的轻量 stdio MCP Server：

* 启动时确保 Daemon 已运行；
* 将 MCP 工具调用转发给 Daemon；
* 不直接加载 Mem0；
* 不直接打开 Qdrant；
* 不直接处理 Embedding。

#### `memory_engine.py`

负责封装 Mem0：

* 创建单例 `Memory`；
* 处理添加、搜索、更新和删除；
* 封装 Provider 差异；
* 固定持久化目录；
* 禁止使用 Mem0 默认的临时 Qdrant 路径。

#### `state_store.py`

使用 SQLite 保存：

* 工作区注册信息；
* Session 与工作区映射；
* Transcript 处理游标；
* 已处理事件哈希；
* 后台失败重试任务；
* Daemon 版本和迁移状态；
* 插件运行状态；
* 非敏感设置。

---

## 五、数据目录设计

默认数据根目录：

```text
~/.mem0/local/
```

目录结构：

```text
~/.mem0/local/
├── venv/
├── config/
├── data/
│   ├── qdrant/
│   ├── mem0-history.db
│   └── state.db
├── runtime/
│   ├── daemon.pid
│   ├── daemon.port
│   ├── daemon.token
│   └── daemon.lock
├── logs/
└── backups/
```

要求：

* `daemon.token` 权限必须限制为当前用户可读。
* API Key 不允许写入日志。
* 插件卸载默认不删除 `data/`。
* 清除数据必须通过明确的管理命令完成。
* 配置、数据、运行状态和日志必须分目录管理。

---

## 六、工作区和通用记忆隔离方案

不要只依赖可被 Agent 提交的 metadata 过滤。

由 Sidecar 内部生成不可伪造的 Mem0 namespace：

```text
通用记忆：
u:{user_hash}:global

工作区记忆：
u:{user_hash}:workspace:{workspace_hash}
```

每条记忆同时保存以下 metadata：

```text
actual_user_id
scope
workspace_id
workspace_name
session_id
memory_type
source
created_at
updated_at
```

### 工作区 ID 解析

继续复用官方 `_project.py` 的以下能力：

1. 显式工作区映射；
2. Git remote；
3. Git 仓库根目录；
4. 当前目录名称作为最后回退。

但需将 `project/app_id` 语义统一改名为 `workspace_id`。官方现有代码已经实现 Git remote、目录和项目映射解析，可以保留主体逻辑。

### 安全规则

* MCP 工具不得接受任意 `user_id`。
* Agent 提交的 `workspace_id` 不得作为可信身份。
* SessionStart 时由 Hook 向 Daemon 注册当前 Session、PID、CWD 和工作区。
* MCP Proxy 根据注册的 Session 获取工作区。
* 工作区不匹配的请求必须拒绝。
* 删除全部记忆必须明确指定 `global` 或当前 `workspace`。
* 删除其他工作区数据必须禁止。
* 删除官方 Skill 中“跨所有用户和项目全局搜索”的能力。

官方的 `enforce_metadata_defaults.sh` 当前主要是给缺失字段补充 `user_id/app_id`，并允许特殊的全局搜索模式；改造后隔离必须转移到服务端，不能只依赖 PreToolUse Hook。

---

## 七、记忆写入流程

### 触发时机

保留：

* `Stop`：一个完整 Assistant 回合结束后捕获。
* `PreCompact`：上下文压缩前强制捕获。
* `SessionStart`：恢复 Session 状态。
* `UserPromptSubmit`：执行召回。

删除官方 `on_user_prompt.sh` 中“每三条用户消息启动一次 auto_capture”的逻辑。当前逻辑发生在 Assistant 回复之前，容易保存不完整回合，并与 Stop 捕获重复。

### Transcript 增量读取

`transcript_reader.py` 必须：

1. 读取 Claude/Codex transcript；
2. 标准化 user、assistant 和 tool 消息；
3. 根据 SQLite 游标只返回新增消息；
4. 生成稳定的事件哈希；
5. 避免同一回合被 Stop、PreCompact 重复写入；
6. 成功处理后再推进游标。

### 两条记忆管线

#### A. 事实记忆

将完整新增对话交给 Mem0：

```text
messages
→ 独立 Memory LLM
→ 事实提取、冲突处理、去重
→ 独立 Embedding
→ Qdrant Local
```

使用 `infer=true`。

#### B. 会话摘要

由独立配置的 Summary LLM 生成结构化摘要：

```text
目标
关键决定
已完成工作
修改文件
已知问题
未完成事项
后续约束
```

摘要结果使用 `infer=false` 写入，类型为：

```text
memory_type = session_summary
```

禁止让 Claude Code/Codex 宿主 Agent生成这份摘要。

### 作用域分类

Memory LLM 同时输出：

```text
scope = global | workspace | discard
```

规则：

* 用户长期偏好、跨项目习惯、通用个人信息：`global`
* 代码、文件、仓库、技术决定、项目任务：`workspace`
* 临时寒暄、一次性操作、低价值输出：`discard`
* 无法明确判断时：默认 `workspace`

---

## 八、记忆召回流程

### `UserPromptSubmit` 新流程

```text
当前用户消息
       │
       ▼
Hook 调用 /v1/recall
       │
       ├── 查询通用 namespace
       ├── 查询当前工作区 namespace
       └── 可选查询当前 session summary
       │
       ▼
合并、去重、排序、Token Budget
       │
       ▼
返回 additionalContext
```

### 检索策略

默认预算建议：

```text
工作区记忆：Top 8
通用记忆：Top 4
Session 摘要：Top 3
最终上下文：不超过 1,600 tokens
```

排序因素：

1. 工作区记忆优先；
2. 向量相似度；
3. importance；
4. 最近使用时间；
5. 记忆类型；
6. 内容去重。

### 查询生成

第一版支持两种模式：

```text
direct：
直接使用当前用户消息生成 Embedding。

rewrite：
使用独立 Recall LLM 生成最多三个检索查询。
```

默认使用 `direct`，配置启用后使用 `rewrite`。

无论哪种模式，都不得要求宿主 Agent 自行生成查询。

官方 `on_user_prompt.sh` 当前会额外向宿主 Agent 注入主动检索 rubric，而 `context-loader` Skill 也要求 Agent 自行生成多条查询；这些逻辑必须移除或改为调用单一的 `recall_context` 工具。

### 故障降级

* 召回超时不得阻塞宿主响应。
* Embedding API 不可用时返回空记忆。
* 失败写入 SQLite 重试队列。
* 不向用户注入 Python traceback。
* 健康检查中显示降级状态。

---

## 九、本地 HTTP 接口

Daemon 至少提供：

```text
GET  /health
POST /v1/session/register
POST /v1/recall
POST /v1/capture
POST /v1/memories/add
POST /v1/memories/search
GET  /v1/memories/{id}
PATCH /v1/memories/{id}
DELETE /v1/memories/{id}
GET  /v1/stats
POST /v1/admin/reload
```

所有接口要求：

* 仅监听 loopback；
* 使用随机本地 Bearer Token；
* 请求体使用 Pydantic 校验；
* 限制请求大小；
* 不允许客户端覆盖内部 namespace；
* 写操作必须具备幂等键。

---

## 十、MCP 工具兼容层

尽量保留官方工具名称：

```text
add_memory
search_memories
get_memories
get_memory
update_memory
delete_memory
delete_all_memories
delete_entities
list_entities
```

新增：

```text
recall_context
memory_health
list_workspaces
consolidate_memories
```

### 工具语义调整

#### `add_memory`

支持：

```text
content
messages
scope = auto | global | workspace
infer = true | false
metadata
```

其中 `auto` 由独立 Memory LLM 分类。

#### `search_memories`

支持：

```text
query
scope = both | global | workspace
top_k
threshold
memory_type
```

不得暴露任意用户搜索。

#### `update_memory`

增加：

```text
metadata_patch
```

这样 `pin` Skill 可以真实更新 metadata，不再通过给正文添加 `[PINNED]` 前缀实现。官方当前 Pin Skill 使用正文前缀模拟固定状态，应当改掉。

#### 异步事件兼容

官方部分 Skills 依赖 `event_id/get_event_status`。第一版可以提供兼容适配：

* 本地同步完成时返回虚拟 `event_id`；
* `get_event_status` 立即返回 `SUCCEEDED`；
* 后续再决定是否改成真正后台任务。

---

## 十一、官方文件级修改清单

### 1. 插件清单与 MCP

| 文件                           | 修改内容                               |
| ---------------------------- | ---------------------------------- |
| `.mcp.json`                  | 将远程 MCP URL 改成本地 stdio MCP Proxy   |
| `.codex-mcp.json`            | 将远程 MCP URL 改成本地 stdio MCP Proxy   |
| `.claude-plugin/plugin.json` | 删除 Mem0 Cloud API Key 必填项，更新本地版说明  |
| `.codex-plugin/plugin.json`  | 更新说明、版本和本地 MCP 入口                  |
| `requirements.txt`           | 增加并锁定 Mem0、Qdrant、MCP、HTTP 服务及配置依赖 |
| `ensure_deps.sh`             | 创建本地 venv、安装锁定依赖、启动 Daemon         |

官方当前依赖安装脚本已经具备持久化 venv、文件锁和依赖 stamp 逻辑，应该保留并扩展，而不是重写整个安装器。

### 2. 搜索和捕获脚本

| 文件                                     | 处理方式                                      |
| -------------------------------------- | ----------------------------------------- |
| `scripts/_search.py`                   | 删除云端请求，改成本地 `/v1/recall` 客户端              |
| `scripts/auto_capture.py`              | 改成 transcript 增量捕获客户端，或合并进 capture client |
| `scripts/capture_session_summary.py`   | 删除云端写入，改为调用 `/v1/capture`                 |
| `scripts/on_user_prompt.sh`            | 只负责召回和注入，不再指示宿主 Agent 搜索或保存               |
| `scripts/on_stop.sh`                   | 调用本地 capture，不依赖 API Key                  |
| `scripts/on_session_start.sh`          | 启动服务、注册 Session、读取本地状态                    |
| `scripts/on_pre_compact*`              | 压缩前提交增量对话                                 |
| `scripts/enforce_metadata_defaults.sh` | 删除身份注入职责，最多保留兼容提示                         |
| `scripts/_identity.py/.sh`             | 改为本地稳定用户 ID，不再检查云端 Key                    |
| `scripts/_project.py/.sh`              | 保留解析逻辑，统一改为 workspace 语义                  |

### 3. 云端专用逻辑

以下内容删除、禁用或改为本地实现：

```text
auto_setup_categories.py
setup_coding_categories.py
云端 onboarding 检查
云端统计接口
云端 telemetry
Mem0 Platform categories
全用户全项目搜索
```

---

## 十二、Skills 修改计划

官方插件当前包含 remember、peek、context-loader、dream、forget、health、stats、onboard、switch-project、memory-reviewer 等 Skills。

### 基本保留并适配

| Skill            | 修改内容                                    |
| ---------------- | --------------------------------------- |
| `remember`       | 调用本地 `add_memory`，默认 `scope=auto`       |
| `peek`           | 使用 `scope=both`，不再手工组装 user/app 过滤      |
| `forget`         | 保留删除确认，限制当前用户和当前作用域                     |
| `export`         | 从本地服务导出                                 |
| `import`         | 导入本地服务并进行作用域确认                          |
| `list-projects`  | 改名或语义调整为 workspace                      |
| `switch-project` | 仅切换本地 workspace，不允许跨用户搜索                |
| `health`         | 检查 Daemon、Qdrant、SQLite、LLM 和 Embedding |
| `stats`          | 使用本地统计接口                                |
| `tour`           | 删除 Platform categories 依赖               |

### 必须重写

#### `context-loader`

不得让宿主 Agent 自己生成 2～4 条搜索查询。

改为：

```text
调用 recall_context
→ Sidecar 自行生成查询
→ 返回融合后的上下文
```

#### `dream`

官方 Skill 当前让宿主 Agent 分析重复和矛盾记忆。改为：

```text
调用 consolidate_memories
→ 独立 Memory LLM 分析
→ 返回修改建议
→ 用户确认
→ Sidecar 执行修改
```

#### `memory-reviewer`

不得让宿主 Agent承担重复、冲突和质量判断。改成只展示 Sidecar 返回的审查报告。官方当前 Dream 和 Memory Reviewer 都由宿主 Agent执行语义分析，与本项目“独立记忆模型”的要求冲突。

#### `onboard`

改为本地初始化向导，只检查：

* Python 环境；
* Daemon；
* 数据目录；
* LLM Provider；
* Embedding Provider；
* 一次写入和召回测试。

不再要求 Mem0 Platform API Key。

---

## 十三、实施阶段

### 阶段 0：建立基线

1. Fork 官方仓库。
2. 记录当前上游 commit。
3. 创建开发分支。
4. 运行官方现有测试。
5. 保存修改前测试结果。
6. 不修改 Mem0 核心库，改动优先限制在 `integrations/mem0-plugin`。

### 阶段 1：实现本地 Memory Core

1. 创建数据目录。
2. 初始化 Mem0 Python Library。
3. 固定 Qdrant Local 持久化路径。
4. 初始化 SQLite State Store。
5. 实现 Provider Factory。
6. 实现 add/search/update/delete。
7. 实现 `/health`。

### 阶段 2：实现 Daemon 与启动器

1. 实现 PID、锁和端口管理。
2. 实现本地认证 Token。
3. 实现首次启动和异常恢复。
4. 实现版本检查和数据库迁移。
5. 实现插件升级后复用旧数据。

### 阶段 3：实现 MCP 兼容层

1. 实现 stdio MCP Proxy。
2. 保留官方工具名称。
3. 更新两份 MCP 配置。
4. 实现 Session 与 workspace 绑定。
5. 禁止工具参数覆盖内部身份。

### 阶段 4：迁移 Hooks

1. SessionStart 启动和注册服务。
2. UserPromptSubmit 调用本地召回。
3. Stop 捕获完整回合。
4. PreCompact 强制捕获。
5. 删除全部 Cloud API 调用。
6. 删除宿主 Agent召回和保存 rubric。

官方 Claude 和 Codex 插件都已经定义了 SessionStart、UserPromptSubmit、Stop、PreCompact 等 Hook，可复用触发时机，只替换脚本内部行为。

### 阶段 5：实现双作用域

1. 生成 global namespace。
2. 生成 workspace namespace。
3. 实现双路搜索。
4. 实现融合排序。
5. 实现作用域自动分类。
6. 实现越权测试。

### 阶段 6：迁移 Skills

1. 先迁移 remember、peek、forget。
2. 再迁移 context-loader。
3. 再迁移 health、stats、onboard。
4. 最后迁移 dream 和 memory-reviewer。
5. 删除所有 Platform categories 和全用户搜索说明。

### 阶段 7：安装、升级和发布

1. 锁定兼容依赖版本。
2. 增加自动安装和修复命令。
3. 增加数据迁移机制。
4. 增加备份和恢复命令。
5. 编写 Claude Code 安装说明。
6. 编写 Codex 安装说明。
7. 验证 Windows、macOS 和 Linux 路径处理。

---

## 十四、测试计划

保留并更新官方现有测试。官方测试目录已经覆盖搜索、自动捕获、Session 总结、项目解析、设置和 Hook rubric，但尚未覆盖本地 Daemon、Qdrant 隔离及 MCP 集成。

新增：

```text
tests/test_daemon_lifecycle.py
tests/test_local_auth.py
tests/test_memory_engine.py
tests/test_scope_isolation.py
tests/test_recall_merge.py
tests/test_capture_delta.py
tests/test_capture_idempotency.py
tests/test_mcp_proxy.py
tests/test_mcp_tools.py
tests/test_provider_factory.py
tests/test_concurrent_clients.py
tests/test_service_restart.py
tests/test_plugin_upgrade.py
tests/test_no_cloud_endpoints.py
tests/test_skill_compatibility.py
```

### 测试要求

* 测试使用临时 Qdrant 和 SQLite 目录。
* 使用 Fake LLM 和 Fake Embedder，不依赖真实 API。
* 固定 Embedding 向量，以验证召回排序。
* 同时启动 Claude 模拟客户端和 Codex 模拟客户端。
* 验证只有一个 Daemon 持有数据库。
* 模拟进程崩溃和服务重启。
* 模拟远程 LLM/Embedding 超时。
* 模拟重复 Stop 和 PreCompact 事件。
* 模拟恶意 workspace ID。
* 扫描源码，确认无 Cloud endpoint。

---

## 十五、验收标准

### 安装与运行

* 安装插件后不需要 Docker。
* 不需要 PostgreSQL。
* 不需要手动启动服务。
* 首次运行自动创建 venv 和数据目录。
* Claude Code 与 Codex 可以共享同一记忆库。
* 同时运行不会出现 Qdrant 文件锁冲突。

### 模型独立性

* Memory LLM 可独立于宿主 Agent 配置。
* Embedding Provider 可独立配置。
* 默认使用远程 API。
* 可切换到本地或自定义 Provider。
* 宿主 Agent不执行记忆提取、总结、分类、查询改写或冲突合并。

### 自动捕获

* 完整 user/assistant 回合结束后自动处理。
* PreCompact 前不会丢失未保存对话。
* 重复事件不会生成重复记忆。
* Transcript 解析失败不会影响宿主正常回答。

### 工作区隔离

在工作区 A 保存：

```text
本项目使用 PostgreSQL
```

在工作区 B 搜索时不得返回该记忆。

在通用作用域保存：

```text
用户偏好使用中文回答
```

工作区 A 和 B 均应能够召回。

伪造其他 workspace ID 的 MCP 请求必须被拒绝或忽略。

### 云端解耦

运行时不得访问：

```text
api.mem0.ai
mcp.mem0.ai
```

不得要求：

```text
MEM0_API_KEY
```

### 数据持久性

* Daemon 重启后记忆仍存在。
* 插件升级后记忆仍存在。
* 插件卸载默认不删除记忆。
* 备份后能够恢复 Qdrant 与 SQLite 数据。

---

## 十六、开发约束

1. 不引入 Docker。
2. 不引入 PostgreSQL。
3. 不把本地 LLM 或模型权重作为默认依赖。
4. 不允许多个进程直接打开 Qdrant Local。
5. 不允许宿主 Agent控制真实 user namespace。
6. 不允许插件更新覆盖用户数据目录。
7. 不记录 API Key、完整对话或认证令牌到普通日志。
8. 不修改 Mem0 核心库，除非确认无法通过 Provider 或包装层实现。
9. 所有新接口必须具备类型校验和错误处理。
10. 所有重要写入必须幂等。
11. 所有 Cloud 功能必须明确删除或提供本地替代。
12. 每个实施阶段使用独立、可回滚的提交。

---

## 十七、交付物

Agent 最终必须提交：

1. 修改后的完整插件源码；
2. 本地 Memory Daemon；
3. MCP Proxy；
4. Claude/Codex Hooks；
5. 更新后的 Skills；
6. 自动安装和启动脚本；
7. 依赖锁文件；
8. 单元测试和集成测试；
9. 本地配置示例；
10. 数据目录和安全说明；
11. 升级、备份和恢复说明；
12. 从官方 Mem0 Cloud 插件迁移到本地版的说明；
13. 修改文件清单；
14. 测试执行结果；
15. 已知限制清单。

---

## 十八、可直接交给 Agent 的执行指令

请基于 Mem0 官方仓库的 `integrations/mem0-plugin` 实施本计划。

先检查并记录上游源码当前 commit 和插件版本，运行现有测试建立基线。改动优先限制在插件目录，不直接修改 Mem0 核心代码。

实现一个由插件自动安装和管理的本地 Python Memory Daemon。Daemon 内嵌 Mem0 Python Library，使用 Qdrant Local 保存向量和记忆，使用 SQLite 保存处理游标、任务、Session、workspace 和运行状态。默认调用可配置的远程 LLM 与 Embedding API，不安装本地模型。

将官方远程 MCP 配置替换成本地 stdio MCP Proxy。MCP Proxy 只转发请求，由唯一的 Daemon 访问 Qdrant 和 SQLite。

迁移 SessionStart、UserPromptSubmit、Stop 和 PreCompact Hooks：

* UserPromptSubmit 调用本地召回；
* Stop 捕获完整回合；
* PreCompact 强制保存尚未处理的对话；
* SessionStart 启动服务并注册 workspace/session；
* 删除所有对 `api.mem0.ai`、`mcp.mem0.ai` 和 `MEM0_API_KEY` 的运行时依赖；
* 删除要求 Claude/Codex 主动生成搜索查询、总结记忆或保存记忆的 rubric。

实现两级记忆作用域：

* global：跨工作区共享；
* workspace：仅当前工作区可见。

作用域必须由服务端 namespace 强制隔离，不得只依赖 Agent 可提交的 metadata。每次召回分别查询 global 和当前 workspace，再完成去重、排序和 Token Budget 控制。

事实提取、会话摘要、作用域分类、召回查询改写和记忆冲突处理必须由独立配置的 Memory/Recall LLM 执行，不得交给宿主 Agent。

保留并适配官方 MCP 工具名称与主要 Skills。重写 context-loader、dream 和 memory-reviewer，使其只调用 Sidecar 能力，不让宿主 Agent承担记忆分析。

实现完整测试，至少覆盖：

* 安装和服务生命周期；
* 无云端访问；
* global/workspace 隔离；
* 双路召回；
* Transcript 增量和幂等；
* 多客户端并发；
* Daemon 重启；
* Provider 切换；
* 插件升级后数据保留；
* 越权 workspace 请求。

完成后输出修改文件清单、架构说明、运行测试结果、尚未解决的问题和安全风险。不要只提供设计或伪代码，必须提交可运行实现。
